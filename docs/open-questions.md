# Open Questions

해결하면 해당 챕터에 반영하고, 필요하면 ADR로 승격한 뒤 여기서 삭제합니다.
아래는 전부 기존 결정에 역류하지 않음을 확인한 항목입니다.

## Blocking

진행을 실제로 막고 있는 항목입니다. **지금은 없습니다.**

## Upstream

우리가 통제하지 않는 항목입니다. **v1 일정을 여기에 묶지 않습니다.**

| 항목 | 내용 |
|---|---|
| 하이퍼링크 노출 | 렌더 상태에 링크 id·URL 노출 요청. 받아들여지면 [0006](adr/0006-no-osc8-in-v1.md)을 재검토 |
| 제목 질의 콜백 | 호스트가 제목 질의 응답을 채우는 콜백. 받아들여지면 [C4](03-core.md#c4--listener-and-response-hygiene)의 바이트 필터를 그 콜백으로 옮김 |

## v1

**비어 있습니다.** M4 스펙이 남은 4건을 닫았습니다.

- **스크롤백 메모리** — 설정 키를 두지 않기로 하면서 소멸했습니다. 사용자가 큰 값을 넣을 길이 없으면 상한 동작을 정할 일이 없습니다.
- **defunct 세션의 표현** — 질문이 이미 답해져 있었습니다. `KtSessionState`(OK/BROKEN)와 `KtChildState`(NONE/RUNNING/EXITED)가 따로 있고, 닫을 때 경고할지는 후자가 입력을 계속 받을지는 전자가 정합니다.
- **스폰 시 작업 디렉터리** — ABI 9에서 `kt_session_new_pty`의 인자로 더합니다.
- **⌘K 대응 경로** — `kt_session_clear` 신설로 갑니다. `clear`는 스크롤백을 지우지 못하므로 셸이 대신할 수 있는 일이 아닙니다.

## v1.5

| 항목 | 내용 | 비고 |
|---|---|---|
| 스크롤백 검색 | 줄 조회 API 필요. 검색 UX 확정 후 설계 | |
| 언더라인 스타일 | 이중·물결. 스냅샷이 이미 다섯을 구분하므로 그리는 쪽만 늘리면 됨 | v1은 전부 단일 실선 |
| 포그라운드 프로세스명 | 종료 경고가 무엇이 돌고 있는지 **이름까지** 말하는 것. 판정 자체는 v1에 있음 | 조회 API 필요 |
| 투명도·블러 | 시스템 시각 효과 레이어와 Metal 레이어 합성 | 창 구조와 엮임 |
| writer 큐 재시도 UI | 배압 시 비프 이상의 UX | v1은 비프로 확정 |
| 탭 재도입 | 자가 사용 중 필요가 제기될 때. cf. [0010](adr/0010-no-tabs-v1.md) | 복원 범위 확장 동반 |
| OSC 8 하이퍼링크 | 상류 노출이 이루어지면 | [0006](adr/0006-no-osc8-in-v1.md) 재검토 |

## v2

범위는 확정, 설계는 미착수입니다.

tmux 컨트롤 모드(`%output` 언이스케이프 — `feed` 경로는 준비됨),
kitty keyboard protocol, OSC 133 셸 통합, 이미지, GUI 설정, 자체 terminfo.

이미지와 kitty keyboard protocol은 VT 엔진에 이미 관련 지원이 있어 비용이 예상보다 낮을 수 있습니다. v2 착수 시점에 확인합니다.
