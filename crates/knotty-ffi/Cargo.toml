[package]
name = "knotty-ffi"
edition.workspace = true
license.workspace = true
publish.workspace = true
version.workspace = true

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
knotty-core.workspace = true

[dev-dependencies]
cbindgen = "0.29"
# Asking whether a child outlived its session, which is a question about a
# process and not about knotty. Already in the graph; cf. the M1 spec,
# `kwnms04/knotty#10`.
rustix.workspace = true
